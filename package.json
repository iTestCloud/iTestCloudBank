{
  "name": "itestcloud-bank",
  "version": "1.0.0",
  "description": "A sample internet banking application for UI automation testing",
  "main": "server/server.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm start --prefix server\"",
    "start": "npm start --prefix server",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
